DIFFERENTIATOR 1.3.46 — SAST / STATIC ASSURANCE SUMMARY

Executed locally on the final 1.3.46 source/build:
- canonical GARP 2.5.1 R2 N20–N22 hotfix tooling selftest: 66/66 PASS;
- GARP 2.3 static/AIR/PC-01 campaign: PASS, including structural AIR variants, marker/operation instruction-boundary cases, fail-closed privacy decision, AI negative control and 7/7 production AI call sites with explicit trusted appInstructions;
- suite-session deterministic simulation: 8/8 PASS including injected cleanup failure behavior;
- repository/build secret scanner: PASS;
- XSS sink regression inventory: PASS against frozen baseline (inventory only; not proof every sink is safe);
- performance/quality gate: PASS; entry HTML remains within the frozen 490,000-byte limit;
- canonical GARP R2 SW freeze: PASS on public and school-server deployment;
- SW negative controls: baseline PASS plus 13 fail-closed mutations, including critical precache, removed/false/negated guard, missing respondWith/return, pre-guard routing/respondWith, ignored pre-guard network/cache effects, multiple fetch handlers, missing cache:no-store and Cache API use inside the trusted sink;
- whole-handler route evidence: all 11 authoritative critical assets route through exactly one networkOnlyNoStore effect; no routing-sensitive read/effect occurs before the guard and no routing metadata is re-read in the critical branch after the guard;
- trusted sink evidence: networkOnlyNoStore performs exactly one fetch with cache:'no-store' and zero Cache API access/write calls;
- vendored consistency: PASS, 9 copies checked, 0 drift;
- AI assurance fingerprint: PASS; canonical verifier rejects untracked AI-boundary candidates (N15 class);
- CycloneDX SBOM coverage: PASS, 67/67 lockfile package paths represented with unique purls and hashes.

This is repository-local bounded static assurance. No claim is made that a commercial SAST engine was executed. Browser/axe/P5 for 1.3.46 and DAST/live-server findings remain separate where not reproduced in this sandbox.
